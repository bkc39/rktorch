Agreed — device.scrbl already says the body runs under autocast per thread
and per device type, and that leaving restores the prior state, so the
comment only repeated it. Removed in 1a31ac3. The comment below it stays:
that one explains why the arguments are checked a second time, which the
code cannot show.
