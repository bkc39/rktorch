Merge master into vision/classic-152

#160 rewrote `define-layer` from a `with-syntax` body into syntax-parse
clauses and gave `#:forward` a rest formal; this branch gave it
contracted formals. Both live in the same hunk, so the resolution takes
the clause form and states the contracts in it:

    #:forward (~or* (input:forward-formal ...)
                    (input:forward-formal ... . restarg:id))

so a layer may contract a formal and take a rest, which neither arc
could express alone; forward-arity-test covers the combination. The
dot-in-a-field-name and bare-field checks this branch raised by hand are
gone: #160's `layer-field` class and `#:fail-when` already make both.

AGENTS.md and torch/nn.rkt take the union of the two arcs' names, and
the predicate list gains `lstm?` and `gru?`, which #160 named
constructors for without listing.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01VDEpCNkMi2rmxjgRnCHhkp
