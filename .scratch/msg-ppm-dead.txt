vision: ppm-dtypes names only the dtypes that exist

float16 and bfloat16 were in the list against #165 landing them, but
nothing on this branch can carry either, so they were unreachable
entries and the manual's "floating-point" overstated what the stack
supports. The list names float32, float64 and uint8; #171 is where
ppm.rkt and the half dtypes first meet, and where they go back in.

`fill-value/c` also gains the `bool-fill-value` name, so that dtype
blames under its own name like int64 and uint8 do.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01VDEpCNkMi2rmxjgRnCHhkp
