review: keep the batch counter in int64, and four smaller fixes

`add` with the scalar 1 promotes the int64 counter to float32, so
BatchNorm stopped counting at 2^24; adding a `ones-like` keeps the
dtype. The other four are from the same review: the `define-layer`
example in nn.scrbl called `batch-norm` in eval mode with no running
statistics, so it raised; `random-crop` and `random-horizontal-flip`
now pass an empty batch through instead of handing `stack` an empty
list; and the comment over `forward-formal` narrated the code.

The promoted `batch-norm` gains the assertion promised on the thread:
an explicit `#:training? #f` without statistics is a contract
violation, not a bypass.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01VDEpCNkMi2rmxjgRnCHhkp
