result
result-*
.racket-user/
cpp/build/
torch/native-libs/libtorchrkt.*
# Host NVIDIA driver symlink farm staged by the `cuda` devShell's shellHook.
.cuda-driver/
**/compiled/
__pycache__/
# train-gpt checkpoint dumps (weights + sidecar)
checkpoints/

# scripts/coverage.rkt's HTML report
coverage/

# Local-only planning notes; agents may drop plan files here without committing.
# (The reviewed v0 plan is checked in as plans/v0-scaffold.md.)
.claude/worktrees/

# exit-loop repro harness output, plus local investigation notes
repro-logs/

# agent scratch: logs, probes and one-off scripts, never part of a change
.scratch/
