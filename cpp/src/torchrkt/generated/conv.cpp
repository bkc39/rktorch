// @generated by codegen (python3 -m codegen); DO NOT EDIT.
// Schema: codegen/aten/native_functions.yaml (libtorch 2.9.0).

#include "torchrkt/c_api/generated/conv.h"

#include <torch/torch.h>

#include "torchrkt/detail/op_call.hpp"
#include "torchrkt/detail/tensor_handle.hpp"

extern "C" {

tr_tensor* tr_gen_adaptive_avg_pool2d(const tr_tensor* self,
                                      const int64_t* output_size,
                                      int64_t output_size_len) {
  if (!self || !output_size || output_size_len < 0) {
    return torchrkt::null_arg("tr_gen_adaptive_avg_pool2d");
  }
  return torchrkt::alloc_result("tr_gen_adaptive_avg_pool2d", [&] {
    return at::adaptive_avg_pool2d(
        self->value,
        at::IntArrayRef(output_size, static_cast<size_t>(output_size_len)));
  });
}

tr_tensor* tr_gen_avg_pool2d(const tr_tensor* self, const int64_t* kernel_size,
                             int64_t kernel_size_len, const int64_t* stride,
                             int64_t stride_len, const int64_t* padding,
                             int64_t padding_len, bool ceil_mode,
                             bool count_include_pad, int64_t divisor_override,
                             bool divisor_override_has) {
  if (!self || !kernel_size || kernel_size_len < 0 || !stride ||
      stride_len < 0 || !padding || padding_len < 0) {
    return torchrkt::null_arg("tr_gen_avg_pool2d");
  }
  return torchrkt::alloc_result("tr_gen_avg_pool2d", [&] {
    return at::avg_pool2d(
        self->value,
        at::IntArrayRef(kernel_size, static_cast<size_t>(kernel_size_len)),
        at::IntArrayRef(stride, static_cast<size_t>(stride_len)),
        at::IntArrayRef(padding, static_cast<size_t>(padding_len)), ceil_mode,
        count_include_pad,
        divisor_override_has ? c10::optional<int64_t>(divisor_override)
                             : c10::optional<int64_t>());
  });
}

tr_tensor* tr_gen_conv1d(const tr_tensor* input, const tr_tensor* weight,
                         const tr_tensor* bias, const int64_t* stride,
                         int64_t stride_len, const int64_t* padding,
                         int64_t padding_len, const int64_t* dilation,
                         int64_t dilation_len, int64_t groups) {
  if (!input || !weight || !stride || stride_len < 0 || !padding ||
      padding_len < 0 || !dilation || dilation_len < 0) {
    return torchrkt::null_arg("tr_gen_conv1d");
  }
  return torchrkt::alloc_result("tr_gen_conv1d", [&] {
    return at::conv1d(
        input->value, weight->value,
        bias ? c10::optional<at::Tensor>(bias->value)
             : c10::optional<at::Tensor>(),
        at::IntArrayRef(stride, static_cast<size_t>(stride_len)),
        at::IntArrayRef(padding, static_cast<size_t>(padding_len)),
        at::IntArrayRef(dilation, static_cast<size_t>(dilation_len)), groups);
  });
}

tr_tensor* tr_gen_conv2d(const tr_tensor* input, const tr_tensor* weight,
                         const tr_tensor* bias, const int64_t* stride,
                         int64_t stride_len, const int64_t* padding,
                         int64_t padding_len, const int64_t* dilation,
                         int64_t dilation_len, int64_t groups) {
  if (!input || !weight || !stride || stride_len < 0 || !padding ||
      padding_len < 0 || !dilation || dilation_len < 0) {
    return torchrkt::null_arg("tr_gen_conv2d");
  }
  return torchrkt::alloc_result("tr_gen_conv2d", [&] {
    return at::conv2d(
        input->value, weight->value,
        bias ? c10::optional<at::Tensor>(bias->value)
             : c10::optional<at::Tensor>(),
        at::IntArrayRef(stride, static_cast<size_t>(stride_len)),
        at::IntArrayRef(padding, static_cast<size_t>(padding_len)),
        at::IntArrayRef(dilation, static_cast<size_t>(dilation_len)), groups);
  });
}

tr_tensor* tr_gen_max_pool2d(const tr_tensor* self, const int64_t* kernel_size,
                             int64_t kernel_size_len, const int64_t* stride,
                             int64_t stride_len, const int64_t* padding,
                             int64_t padding_len, const int64_t* dilation,
                             int64_t dilation_len, bool ceil_mode) {
  if (!self || !kernel_size || kernel_size_len < 0 || !stride ||
      stride_len < 0 || !padding || padding_len < 0 || !dilation ||
      dilation_len < 0) {
    return torchrkt::null_arg("tr_gen_max_pool2d");
  }
  return torchrkt::alloc_result("tr_gen_max_pool2d", [&] {
    return at::max_pool2d(
        self->value,
        at::IntArrayRef(kernel_size, static_cast<size_t>(kernel_size_len)),
        at::IntArrayRef(stride, static_cast<size_t>(stride_len)),
        at::IntArrayRef(padding, static_cast<size_t>(padding_len)),
        at::IntArrayRef(dilation, static_cast<size_t>(dilation_len)),
        ceil_mode);
  });
}

}  // extern "C"
